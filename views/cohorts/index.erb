<div class="container index-form">
  <h1>Cohorts</h1>
  <table class="table">
    <thead class="thead-dark table-headers">
      <tr>
        <th>Cohort Name</th>
      </tr>
    </thead>
    <tbody>
          <% @cohorts.each do |cohort| %>
            <tr>
              <td><%= cohort.cohort_name %></td>
            </tr>
          <% end %>
    </tbody>
  </table>
  <button class="btn btn-success btn-lg btn-block" type="button" onclick="window.location.href = 'cohorts/new';" name="button">Add New</button>
</div>
